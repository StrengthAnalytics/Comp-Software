// The public live scoreboard / standings for venue TVs and social shares (planned; stub for now).
// It previously sat at /[comp-slug]/live, but that URL now serves the public warm-up board — the
// comp's primary public live view — so this moved to /standings. (/scoreboard is taken by the OBS
// overlay source.)
export default function LiveScoreboardPage() {
  return <h1>Live scoreboard</h1>;
}
